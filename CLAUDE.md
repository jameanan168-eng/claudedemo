# CLAUDE.md — Mew's Claude Code Guidelines

> 📝 **ไฟล์นี้คืออะไร** — "กฎกลาง" ที่กลั่นจากระบบ production จริง เพื่อให้คนที่ไม่ใช่ dev ก็ build ระบบใช้งานได้
> 🚀 **วิธีใช้:** เปลี่ยนชื่อไฟล์เป็น `CLAUDE.md` → วางในโฟลเดอร์โปรเจกต์ → Claude Code อ่านเองทุกครั้ง

---

## 1 · คุยกับฉันยังไง
- **ผู้ใช้ไฟล์นี้ไม่ใช่โปรแกรมเมอร์** — อธิบายด้วยภาษาคน ไม่ร่ายศัพท์เทคนิค
- ก่อนลงมือ บอกสั้น ๆ ว่า กำลังทำอะไร · ทำไม · ผลที่จะได้
- ตอบภาษาเดียวกับที่เจ้าของพิมพ์ · ฟันธง ไม่เวิ่นเว้อ · ไม่ชัดถามกลับเป็นไทย

---

## 2 · Stack มาตรฐาน
ถ้าไม่ได้บอกเป็นอย่างอื่น **ใช้ชุดนี้** (เสถียร ต่อยอดง่าย):

| งาน | ใช้ |
|---|---|
| โครงเว็บ/แอป | **Next.js (App Router)** |
| ภาษา | **TypeScript** (strict — ห้าม `any` ลอย ๆ) |
| หน้าตา | **Tailwind CSS** + **shadcn/ui** |
| ฐานข้อมูล | **Postgres** ผ่าน **Prisma** (เช่น Neon) |
| ส่งอีเมล | **Resend** · ตรวจข้อมูล | **zod** · รับเงิน | **Stripe** |

> ⚠️ **เวอร์ชัน — ระบุให้ชัด อย่าคว้า `@latest` (สำคัญมาก)**
> major version ที่ "เพิ่งออก" มักมี breaking change + เอกสาร/ความรู้ AI ตามไม่ทัน → พังกลางทางง่าย
> · ติดตั้งระบุเวอร์ชันเสถียรเสมอ เช่น `prisma@6` ไม่ใช่ `prisma@latest`
> · **ค่าแนะนำตอนนี้: Next.js 15 · Prisma 6 · Tailwind CSS 3**
> · อยากใช้ใหม่กว่า → เช็คก่อนว่าออกมานานพอ เอกสารครบ

- เขียน **server component** เป็นหลัก · logic ที่แตะ DB/กุญแจลับ อยู่ฝั่ง server เท่านั้น

---

## 3 · ฐานข้อมูล
- แก้ตารางผ่าน **Prisma migration** เสมอ · หลังตั้งตาราง **ใส่ข้อมูลตัวอย่าง (seed)**
- ทุกการอ่าน/เขียนข้อมูล วิ่งผ่าน API ฝั่ง server ที่เดียว · connection เก็บใน `.env.local`

## 4 · ความปลอดภัย
- ❌ ห้ามเขียน key/รหัสติดในโค้ด — เก็บ `.env.local` ห้าม commit
- ก่อนรันคำสั่งลบ/ทับของได้ ต้องเตือนเป็นไทย + ขอ "ตกลง" ก่อน

## 5 · ทดสอบก่อนส่ง
- ก่อน push git **ต้องผ่าน lint · typecheck · build** · ไม่ผ่าน = หยุด แก้ก่อน

## 6 · dev server
- เช็คก่อนว่า dev server เปิดอยู่ไหม · ❌ ห้าม build ตอน dev กำลังรัน

## 7 · วินัยการทำงาน
- ทำเสร็จแจ้งก่อน ห้าม push เอง · ❌ ห้ามลูกเล่นเร่งขาย · ❌ ห้าม hardcode ตัวเลขสำคัญ

## 8 · ต่อยอดเป็น PRD.md
แต่ละโปรเจกต์มี `PRD.md` ตอบ "ทำอะไร" → สั่ง Claude "ถามทีละข้อตามโครง PRD แล้วเขียนให้"

## 9 · วิธีดูแลไฟล์นี้
เจอกฎใหม่ที่ใช้ทุกงาน → เพิ่ม · ไม่ใช้แล้ว → ลบ · รีวิวทุก 2–4 สัปดาห์
